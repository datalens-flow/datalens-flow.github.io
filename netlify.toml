[build]
  base = "frontend"
  publish = "dist"
  command = "npm run build"

# Redirect all other paths to index.html for React Router SPA support
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
